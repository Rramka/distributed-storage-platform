# DSP weekly — 2026-09-22

## This week
M0–M4 is the raise artifact: a clean Compose fleet, a committed file, kill 6 of 16, download during the outage, repair back to 16/16, `pg_dump` decrypts nothing.

- Wiped the dirty volume that had placement-cap violations from W11 soak fixtures. Fresh `dsp put` + `make invariants` was clean.
- Recorded `make demo` against that fleet (metrics in `demo-metrics-2026-09-22.json`). Clock starts at kill and includes the 5 min offline grace.
- Filled sourced competitor/pricing notes, a slide outline, a data-room index, and a static waitlist page (Formspree; no control-plane table).

## Demo metric
`business/updates/demo-metrics-2026-09-22.json`: 31 MiB file, kill 6, **download during failure: true**, **16/16** in **296s** from kill (includes the 5 min offline grace), **pg_dump decrypts nothing**.

W11 soak (not this week’s take): 2h, min healthy 10/16, floor held; kill withheld while already ≤12.

## Next week
The remaining risk is the screen recording itself — the harness run is the proof; the video is the email. Record from `demo-script-w12.md` with the visualizer on one half of the screen.

## Ask
One intro to an angel who has written a storage or infra check and will watch a 90-second terminal + visualizer clip.
